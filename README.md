# AskPerplexity-DiscordBot
A Discord bot that lets users ask questions directly in Discord channels using the !ask command, which then queries Perplexity AI's web-search-enabled model and returns answers in real-time. It automatically handles long responses by splitting them into multiple messages and includes error handling for API failures.
